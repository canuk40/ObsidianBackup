// health/HealthDataExporterTest.kt
package com.obsidianbackup.health

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Test
import org.junit.runner.RunWith

/**
 * Health Connect integration tests — disabled pending org account setup.
 * Tests will be re-enabled in a future release when Health Connect is restored.
 */
@RunWith(AndroidJUnit4::class)
class HealthDataExporterTest {

    @Test
    fun healthConnectIntegrationRemoved() {
        // Health Connect removed — pending org account setup
        // This feature will be re-enabled in a future release
    }
}
